# 발표자료 디자인 시스템 — 지면(誌面)

권규상 교수의 발표 HTML 슬라이드가 따르는 규범이다.
슬라이드를 만들거나 고치기 전에 이 문서를 읽는다.

**§1~§11은 `../200_education/design.md`(강의노트)를 2026-09-05에 그대로 복사한 것이다.**
두 프로젝트가 같은 지면 규범을 쓴다. 그 절들의 규칙을 바꿔야 하면 **200_education 쪽을
먼저 고치고 여기로 다시 복사한 뒤 §12를 붙인다.** 여기서만 고치면 두 문서가 갈라진다.
CSS도 같은 방향이다 — `_scripts/sync-shared.sh` 참조.

§1~§11이 **`deck-init.js` · `course.json` · 과목 · 학기 · 강**이라고 적은 자리는 여기서
각각 **`talk-init.js` · `talk.json` · 행사 · 발표 · 덱**으로 읽는다. 규범은 그대로다.

**발표에만 해당하는 것은 §12에 있다.** 표지 구성, 로고 줄, 푸터 문구, 시간 예산이
거기 있고, 그 근거가 되는 CSS는 `_shared/theme/talk.css` 하나뿐이다.

**이 문서는 2026-08 전면 개편본이다.** 그 전 판은 기존 PowerPoint 원본에서 실측한
치수·색을 규범으로 삼았는데, 1~9강이 전부 HTML로 옮겨진 뒤로 맞춰야 할 PPT가
없어졌다. 지금 규범의 근거는 원본이 아니라 **읽히는 지면**이다.

성격은 한 문장으로 이렇다. **학술 저널의 편집 지면.** 장식을 걷어내고,
활자와 여백과 그림이 스스로 위계를 만든다.

---

## 1. 원칙

1. **한 슬라이드에 하나의 초점.** 같은 크기의 덩어리 넷이 있으면 대개 실패한다.
2. **색은 뜻으로만 쓴다.** 장식으로 쓰지 않는다. 화면 대부분은 무채색이고
   크림슨은 정말 필요한 곳에만 나타난다.
3. **뜻이 다른 곡선은 색이 분명히 갈려야 한다.** 차분함보다 구분이 먼저다.
   발표장 뒷자리에서 두 곡선이 붙어 보이면 그 슬라이드는 실패한 것이다.
4. **유형마다 밀도가 다르다.** 정의 슬라이드는 비우고, 표·그래프 슬라이드는 채운다.
   섹션 구분은 거의 텅 빈다.
5. **그림은 데이터에서 유도한다.** 좌표를 손으로 찍지 않는다 (§6.4).
6. **넘치면 고친다.** 표·그림이 푸터와 겹치는 것은 오류다 (§4.4).

---

## 2. 좌표계와 서체

### 2.1 슬라이드 크기

`960 × 540` (16:9). Reveal.js를 `width: 960, height: 540`으로 설정한다.
CSS 픽셀 1 = 슬라이드 좌표 1이므로 이 문서의 모든 수치는 그대로 CSS 값이다.

### 2.2 서체

| 쓰임 | 서체 | 파일 |
|---|---|---|
| 슬라이드 제목 · 표지 제목 · 섹션 라벨 · Q&A | **부크크 명조 Bold** | `_shared/fonts/BookkMyungjo-Bold.woff2` |
| 그 밖의 모든 것 (본문·표·그래프·푸터) | **Pretendard** 400/500/700 | `_shared/fonts/Pretendard-*.woff2` |

제목에만 명조를 쓴다. **본문에는 쓰지 않는다** — 프로젝터에서 얇은 획이 날아간다.

글꼴은 단일 woff2 파일로만 벤더링한다. Google Fonts는 한글을 unicode-range로
100개 넘게 쪼개 배포하므로 Drive 폴더에 쓸 수 없다 (CLAUDE.md §2).

### 2.3 타이포 스케일

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--fs-cover` | 46px | 표지 제목 (명조) |
| `--fs-title` | 31px | 슬라이드 제목 (명조) |
| — | 11px | kicker (강 번호 + 강의 제목) |
| `--fs-lead` | 20px | 정의문·발문 |
| `--fs-sub` | 16px | 소제목 (크림슨) |
| `--fs-emph` | 16px | 강조 본문 |
| `--fs-body` | 15px | 본문 |
| `--fs-caption` | 12px | 캡션·출처 |
| `--fs-footer` | 10px | 하단 메타 |

`--lh-body: 1.62`. 제목은 1.18, 자간 −0.035em.

---

## 3. 색

### 3.1 토큰

```
--ink            #16181C   본문 잉크 · 기본 곡선 · 축        17.8:1
--ink-2          #4A4741   본문 문단                          9.3:1
--muted          #8A867E   캡션 · 하단 메타                   3.6:1
--rule           #E2DED7   헤어라인 (푸터선 · 표 괘선)
--guide          #C9C5BE   그래프 점선 보조선

--brand-crimson  #A02948   메인색                             7.2:1
--accent-blue    #1F5C8B   수요곡선 · 캡션                    7.1:1
--accent-green   #2E7D4F   3계열 곡선                         5.1:1
--chart-orange   #B07A1E   대비 곡선                          3.7:1
```

대비비는 흰 배경 기준. `--muted`와 `--chart-orange`는 3:1대이므로
**작은 본문에 쓰지 않는다.** 큰 글자나 도형에만 쓴다.

### 3.2 메인색을 이렇게 정한 이유

충북대 마크색은 **PANTONE 7425 C = C20 M95 Y45 K10 = `#B72654`**(색상각 341°)다.
그런데 화면에서 자홍 기가 강하게 읽혀, 이전에 쓰던 `#892B3C`(349°)와의
**중간값 `#A02948`**을 메인색으로 삼았다.

마크색 원값이 아니라는 점을 알고 쓰는 것이다. 되돌리지 않는다.

### 3.3 곡선의 색 — variant 매핑

`Fig.curve(pts, { variant: '...' })`의 variant가 곧 색이다.

| variant | 색 | 쓰임 |
|---|---|---|
| (없음) | `--ink` | 기본 곡선 · 주인공이 하나뿐일 때 |
| `demand` | `--accent-blue` | 수요곡선 · 대비되는 쪽 |
| `supply` | `--brand-crimson` | 공급곡선 · 결론이 되는 쪽 |
| `i1` | `--accent-green` | 3계열 중 하나 |
| `i2`, `alt` | `--chart-orange` | 3계열 중 하나 · 이동 후 곡선 |
| `i3`, `budget` | `--accent-blue` | 예산선 · 등비용선 같은 보조 직선 다발 |
| `cap` | `--muted` | 가격상한선 · 배경으로 물러나는 선 |

**한 그림 안에서 뜻이 다른 두 곡선이 같은 variant를 쓰면 안 된다.**
같은 뜻의 곡선 다발(예산선 3개, 등비용선 3개)은 같은 variant가 맞다.

variant 가 달라도 색이 겹칠 수 있다 — `--accent-blue` 와 `--chart-blue` 는 같은 값이라
`demand` 와 `i3`·`budget` 을 한 그림에 쓰면 같은 파랑이 된다.
**2026-08 부터 `check-figures.sh` 가 이것을 기계로 잡는다** (같은 색 + 다른 variant 조합).
다만 라벨 겹침·경제 관계의 정합성은 여전히 눈으로 확인한다.

### 3.4 채움

```
.area-surplus   크림슨 10%    잉여 영역
.area-loss      크림슨 28%    손실 영역
.area-bar       크림슨 18%    막대
.area-dwl       파랑  30%     후생손실 삼각형
```

---

## 4. 레이아웃

### 4.1 본문 슬라이드

```
 y=28   kicker      "03. 소비자 이론과 생산자 이론"  11px 크림슨 자간 .14em
 y=62   제목        31px 명조
        ↓ 13px
        tick        84 × 2px 크림슨 — 전폭 룰을 쓰지 않는다
 y=134  본문 시작
 …
 y=500  본문 끝
 y=510  헤어라인 + 하단 메타 (좌: 과목·학기 / 우: 쪽번호)
```

좌우 여백은 `--gutter-l/r: 56px`. 본문 폭 848px.

kicker와 하단 메타는 `deck-init.js`가 `course.json`을 읽어 넣는다.
**슬라이드 HTML에 강의 제목·과목명·날짜·쪽번호를 적지 않는다.**

### 4.2 크림슨 푸터바를 쓰지 않는다

이전 판은 하단 전면에 크림슨 밴드를 깔았다. 그것이 이 강의노트를
가장 낡아 보이게 하던 요소였다. 지금은 헤어라인 한 줄과 10px 회색 글자다.

### 4.3 유형별 밀도

| 유형 | 밀도 |
|---|---|
| 표지 | 중간. 큰 제목 하나 + 날짜 |
| 정의 | 가장 낮다. 아래를 비워도 좋다 |
| 개념 + 그래프 | 높다. 그림이 화면의 절반 이상 |
| 표 | 높다 |
| 섹션 구분 | 거의 빈다. "리듬의 재설정"이지 두 번째 표지가 아니다 |

**이전 판의 "아래를 비워두지 않는다"는 규칙은 폐기했다.** 모든 슬라이드를
같은 밀도로 채우면 위계가 사라진다.

**표지 제목은 46px — 한 줄에 14자 남짓이다.** 자동 줄바꿈에 맡기면
"균/형", "정/부실패"처럼 낱말이 갈라진다. 긴 제목은 뜻 단위로 `<br>`을 직접 넣는다.

**표에는 폭을 명시한다.** `width` 없는 `<table>`은 shrink-to-fit이라 칸이 좁아지고
글이 여러 줄로 접혀 표 높이가 두세 배가 된다 — 그대로 푸터를 넘는다.
`width: 100%; table-layout: fixed`를 주고 첫 칸 폭을 못 박는다.
칸 폭은 px보다 %가 낫다 — 좁은 단(`.cols` 안)에 들어가도 접히지 않는다.

**글만 있는 2단은 `.cols`를 위 정렬로 바꾼다.** `components.css`의 `.cols`는
그림과 글을 나란히 놓는 용도라 `align-items: center`다. 양쪽 다 글이면
짧은 칸이 세로 가운데로 밀려 떠 보인다. 강의 `<style>`에서
`.body > .cols { align-items: flex-start; }` 로 덮는다.

**`.pd-lead`·`.pd-sub`·`.s-numbered`는 새 강의에서 쓰지 않는다.**
앞 둘은 옛 제목 좌표(23px) 기준의 절대배치라 지면 테마에서 제목을 덮고,
`.s-numbered`의 40px 항목 간격은 `ol.dense`(0,1,1)를 가중치로 이겨 조여지지 않는다.
발문은 `.body.stack` 안의 `.lead.t-define`으로, 긴 목록은 일반 `<section>` + `ol.dense`로 쓴다.

**과목 전용 도형 폭을 `components.css`에 추가하지 않는다.** 1학기 때 `.s-pair #f-se-m` 식
id 규칙이 20줄 쌓였다. 새 과목은 `svg.fig`에 인라인 `style="width:…px"`로 준다.

### 4.4 푸터를 넘지 않는다

표·그림이 하단 헤어라인과 겹치면 강의실에서도 PDF에서도 잘려 보인다.
`_scripts/check-figures.sh`가 슬라이드 번호와 넘친 픽셀까지 찍어 준다.

```
14번 슬라이드: svg#f-se 가 푸터를 39px 넘었습니다
```

**0건이 될 때까지 PDF를 뽑지 않는다.** 고치는 순서는 이렇다.

1. 그림에는 이미 상한이 걸려 있다 — `.body svg.fig`는 330px,
   `.cols` 안이면 296px. 대개 이걸로 해결된다.
2. 글이 많아 넘치면 `<div class="body tight">`, 그래도 넘치면 `tighter`.
3. 그래도 안 되면 그림·표 자체를 줄인다.

### 4.5 출처는 그 그림 아래에 적는다 (2026-08-28)

그림·사진·도식·표의 출처는 **그 자리 바로 아래**에 적는다. 슬라이드 맨 뒤에
"자료 출처" 장을 따로 두지 않는다 — 인용한 자리에서 보이지 않으면 아무도 대조하지 않는다.

```html
<p class="caption">주: 측정 방식이 판마다 달라 절대 비교에는 한계가 있다</p>
<p class="caption">출처: Anthropic 공개 자료 <span class="u">anthropic.com</span></p>
```

- 양식은 `출처: 내용` 이다. 설명이 더 필요하면 **출처 위에** `주: 내용` 을 먼저 둔다.
- 주소는 `<span class="u">` 로 감싼다.
- 한 줄에 들어가게 줄인다. 목록이 길면 대표 주소 하나로 묶는다.
- 직접 그린 도식에는 출처를 적지 않는다.

### 4.6 제목은 한 줄로 쓴다 (2026-09-01)

표지의 `<h1>` 과 본문의 `<h2>` 는 **너무 길지 않으면 한 줄로** 둔다.
`<br>` 로 두 줄을 만들지 않는다. 한 줄에 안 들어갈 만큼 긴 제목만 예외다.

```html
<h1>1강. 도시교통통계학 강의 소개</h1>      <!-- 이렇게 -->
<h1>1강. 도시교통통계학<br>강의 소개</h1>   <!-- 이렇게 말고 -->
```

기존 노트에는 두 줄 표지가 아직 남아 있다. **일괄로 고치지 않는다** —
그 강의를 손볼 때 요청이 없어도 함께 고친다 (2026-09-01).

### 4.7 본문은 개조식으로 쓴다 (2026-09-01)

슬라이드 본문은 **명사로 끝내는 것을 기본**으로 한다. 명사로 끊기 어색하면 `~함` 을 쓴다.
`~이다` · `~한다` 같은 서술형 종결은 쓰지 않는다. 강의노트는 읽는 글이 아니라 보는 판이다.

```html
<p>편익과 비용을 견주어 이주를 결정한다</p>   <!-- 이렇게 말고 -->
<p>편익과 비용을 견주어 이주 결정</p>          <!-- 이렇게 -->
```

발문·인용문처럼 문장이 그대로 살아야 하는 자리는 예외다.
기존 노트는 일괄로 고치지 않는다 — §4.6 과 같이 그 강의를 손볼 때 함께 고친다.

### 4.8 문장을 손으로 줄바꿈하지 않는다 (2026-08-28)

`<br>` 로 문장 가운데를 끊지 않는다. 한 문장은 한 줄로 쓰고, 길면 상자 폭에 맞춰
저절로 접히게 둔다. 손으로 끊으면 글자 크기나 폭이 바뀔 때마다 어긋난다.
문장이 여럿이면 문장마다 `<p>` 를 쓴다. `&nbsp;` 로 들여쓰기를 흉내 내지 않는다.

예외는 `.define` 처럼 활자가 큰 표제 문장이다. 어차피 두 줄로 접히므로
**의미가 끊기는 자리에서 한 번** 손으로 끊는 편이 읽기 좋다 (1강 3쪽 목표 문장). 제목은 §4.6 을, 종결은 §4.7 을 따른다.

---

## 5. 슬라이드 유형 카탈로그

| 코드 | 클래스 | 쓰임 |
|---|---|---|
| T1 | `.s-cover` | 표지 |
| T2 | `.s-question` | 발문 |
| T3 | (기본) | 정의 — `.lead.t-define` |
| T4 | `.s-split` | 좌 설명·표 / 우 그래프 |
| T5 | (기본) | 그래프 단독 + 주석 |
| T6 | `.s-triptych`, `.triptych` | 3분할 비교 |
| T7 | (기본) | 수식 — `.formula-key` |
| T8 | (기본) | 표 |
| T9 | `.fragment` | 누적 공개 |
| T10 | `.s-closing` | Q & A |
| T11 | `.s-numbered` | 번호 목록 |
| T12 | `.s-section` | 섹션 구분 |
| — | `.s-pair` | 두 판을 나란히 (시장 / 개별기업) |
| — | `.corner-note` | 우상단 보조 노트 |

`.body`에 붙이는 변형: `.stack`(세로 가운데), `.tight`·`.tighter`(조임),
`.cols`(정의문이 전폭을 쓰고 그 아래에서 좌우 분할).

---

## 6. 그래프

### 6.1 개념도는 인라인 SVG로 그린다

강의노트의 곡선은 실측 데이터가 아니라 개념도다. 차트 라이브러리를 쓰면
축·격자·범례가 딸려 나온다. `_shared/js/charts.js`의 `Fig`로 직접 그린다.

```html
<svg class="fig" id="f-mc" viewBox="0 0 520 330" style="width:480px" role="img">
  <title>무엇을 그린 그림인지 반드시 적는다</title>
</svg>
<script>
  (function () {
    const F = Fig.withBox({ x0: 60, y0: 274, x1: 430, y1: 26 });
    const S = F.scale({ x: [0, 8.4], y: [0, 400] });
    F.draw('#f-mc', { axes: { x: '생산량', y: '비용' }, parts: [ … ] });
  })();
</script>
```

`<title>`은 스크린리더가 읽는다. 빠뜨리지 않는다.

### 6.2 규격

| 요소 | 값 |
|---|---|
| 곡선 | 2.4px (계열 곡선 2.2px) |
| 축 | `--ink` 1px |
| 보조선 | `--guide` 1.2px, dash 3 3 |
| 좌표점 | `--brand-crimson` r=4.5 |
| 속 빈 점 | 흰 채움 + `--ink-2` 1.4px 테두리 |
| 축 이름·눈금 | 11px |
| 주석 | 12px |

**1px 저대비 선을 쓰지 않는다.** 압축된 화면과 프로젝터에서 사라진다.

### 6.3 Chart.js는 실제 데이터일 때만

통계·실증 자료에만 `_shared/vendor/chartjs/chart.umd.min.js`를 쓴다.
`animation: false`로 둔다 — PDF 캡처가 흔들린다.
`devicePixelRatio: 3` 도 함께 준다 — decktape 은 화면 해상도 그대로 잡으므로
이걸 빼면 PDF 에서 캔버스만 흐릿하다 (SVG 도식은 벡터라 영향이 없다).

### 6.4 좌표는 손으로 찍지 않는다 — 데이터에서 구한다

이 규칙 하나가 이 프로젝트에서 가장 많은 오류를 막았다.

```js
const S = F.scale({ x: [0, 10], y: [0, 400] });   // 데이터 → 픽셀
S.p([3, 40])                                       // 점
S.curve([[1, 360], [2, 205], …])                   // 곡선
```

곡선과 눈금과 점이 **같은 표의 숫자에서** 나오므로 어긋날 수 없다.

| 함수 | 쓰임 |
|---|---|
| `Fig.at(pts, {x｜y})` | 곡선 위의 점 |
| `Fig.cross(a, b)` | 두 곡선의 교차점 (MR=MC, 수요=공급) |
| `Fig.tangentU(L, dL, t, half, k)` | 포락선 `L`에 접하는 U자 |
| `Fig.hyper(k, x0, x1, {through})` | 직각쌍곡선 (무차별곡선·등량곡선) |
| `Fig.shift(pts, dx, dy)` | 곡선 이동 |
| `Fig.scatter({n, mx, my, sx, sy, r, seed})` | 산점도 표본 — 표본평균·표준편차·상관계수가 지정값과 정확히 일치한다 |
| `Fig.dots(pts)` | 산점도의 점들 (`.pt`). 곡선 위에 있어야 하는 `Fig.dot`(`.dot`)과 달리 검사 대상이 아니다 |

**주의할 것들.** (전부 실제로 겪었다)

- `Fig.cross`는 **수직선끼리 못 구한다.** 화폐공급곡선처럼 수직인 선은
  `Fig.at(상대곡선, {x})`로 구한다.
- `Fig.at`은 다각선을 선형보간하는데 `curve()`는 스플라인으로 그린다.
  **점이 성기면 둘이 어긋난다.** 곡선을 함수로 정의해 촘촘히 샘플링한다.
- **꺾인 선(V자 순가격곡선, 고속도로 지대곡선)을 `curve()`로 그리지 않는다.**
  스플라인이 꼭짓점을 둥글게 깎아 그 위의 점이 검사에서 벗어난다(2학기 2강에서 12.7px).
  `line()` 두 개로 그린다.
- **직선도 축 범위를 넘는 구간은 잘라서 그린다.** 기울기가 큰 수요·공급 직선을
  플롯 밖까지 그리면 선 끝과 라벨이 viewBox 를 벗어난다. 경계에서의 x 를 풀어
  보이는 구간만 `line()` 한다.
- **유도한 수치를 화면에 찍을 때는 반올림한다.** `1.8/10` 은 `0.18000000000000002`,
  `100×1.12` 는 `112.00000000000001` 로 인쇄된다. 상수 객체에 반올림 도우미를 두고
  내보내는 값마다 통과시킨다.
- **곡선이 셋 이상이면 색을 눈으로 확인한다.** 특히 지면 팔레트에서
  `--accent-blue` 와 `--chart-blue` 가 같은 값이라 `demand` 와 `i3`·`budget` 을
  한 그림에 쓰면 겹친다 (2학기 6강에서 실제 발생 — `i1` 초록으로 회피).

### 6.5 경제 관계는 관계식에서 유도한다

곡선을 눈대중으로 그리면 경제학적 관계가 조용히 깨진다. 실제로 겪은 것들이다.

- **MC는 AC·AVC의 최저점을 지난다.** 세 곡선을 따로 찍으면 어긋난다.
  `TC = F + aq³ − bq² + cq` 하나에서 전부 유도한다.
- **한계수입은 수요곡선에서 나온다.** `P = a − bQ`면 `MR = a − 2bQ`.
  절편은 같고 기울기가 두 배다.
- **LAC는 SAC를 가로지르지 않는다.** SAC를 먼저 그리고 최저점을 이으면
  반드시 뚫는다 — 포물선은 최저점 근처가 평평하기 때문이다.
  LAC를 먼저 정하고 `Fig.tangentU`로 SAC를 얹는다.
- **독점경쟁 장기균형**은 ① 수요곡선이 LAC에 접하고 ② MR = LMC인 곳이
  **같은 생산량**이어야 한다. 같은 비용함수에서 유도하면 자동으로 맞는다.

### 6.6 본문 강조 — `.mark` 와 `.hl`

강조 클래스가 둘이다. **`.mark` 는 크림슨 + 밑줄**, **`.hl` 은 크림슨만**이다.
원본 PPT 의 붉은 강조에는 밑줄이 없는 경우가 많아 그때는 `.hl` 을 쓴다.

`.hl` 은 표 셀에서도 쓰인다(정규분포표에서 찾은 칸을 표시하는 식). 그래서 공용 규칙은
`.reveal .hl`(0,2,0)로 두었다 — `.hl`(0,1,0)로 두면 `.reveal table td`(0,1,2)에 져서
**표 안에서만 색이 조용히 안 먹는다.** 실제로 3강이 그 상태였다.

### 6.7 그래프 안 흰 글자는 `.on-dark`

`svg.fig text` 는 모든 글자에 흰 테두리 3px 을 두른다 — 곡선·보조선 위에 글자가 얹혀도
읽히게 하려는 안전장치다. 그런데 **크림슨·회색 면 위에 얹는 흰 글자**에는 역효과가 난다.
흰 테두리가 자간을 메워 글자가 한 덩어리로 뭉개진다.

그런 글자에는 `class="on-dark"` 를 준다. 두 학기 8개 강의가 각자 `stroke:none` 을 껐던
자리라, 이름을 하나로 모았다.

```html
<text class="on-dark" x="..." y="..." fill="#FFF">자료</text>
```

### 6.8 겹침 — 먼저 피하고, 마지막에 가린다

라벨 위치를 먼저 조정한다. 그래도 겹치면 흰 테두리가 마지막 안전장치다
(`paint-order: stroke fill`). 테두리를 믿고 겹쳐 놓지 않는다.

### 6.9 채움 영역 안의 라벨은 `Fig.labelIn()`

무게중심에 놓은 뒤 네 모서리가 다각형 안에 들어올 때까지 자동으로 옮기고
줄인다. 삼각형 빗변을 넘는 사고를 막는다.

### 6.10 검사

`_scripts/check-figures.sh`가 기계로 잡는 것:

- `[Fig]` 경고 — 점이 곡선에서 2.5px 이상 벗어남
- 글자가 viewBox 밖으로 나감
- `labelIn` 라벨이 채움 영역 밖으로 삐져나옴
- **표·그림이 푸터를 넘음**
- **뜻이 다른 곡선이 같은 색** (다른 variant 가 같은 stroke 로 계산되는 조합)

잡지 못하는 것: 라벨끼리의 겹침, 경제 관계의 정합성, 문구.

---

## 7. 수식

MathJax 3 `tex-svg.js` (자체 완결 단일 파일). MathJax 4는 `sre/` 음성 파일을
지연 로드하다 404를 낸다.

`\color{}`를 **쓰지 않는다.** 별도 확장 파일이 필요해 수식 전체가 깨진다.
색은 바깥에서 준다 — SVG 출력이 `currentColor`를 따른다.

```html
<!-- 안 된다 -->  $\color{#A02948}{Y = \frac{1}{1-b}}$
<!-- 된다   -->  <p class="formula-key">$Y = \dfrac{1}{1-b}$</p>
```

---

## 8. 누적 공개

원본 PPT에서 같은 제목이 연속으로 나오고 요소만 늘어나면, 그것은 한 슬라이드의
단계다. **슬라이드를 복제하지 않고 `.fragment`로 만든다.**

```js
F.annot(x, y, '→ 한계편익', { tone: 'red', fragment: 0 })
```

Reveal은 `pdfSeparateFragments: true`, decktape는 `--fragments`로 돌린다.
그래야 fragment 한 단계가 PDF 한 쪽이 되어 **원본과 쪽수가 맞는다.**

쪽번호는 슬라이드가 아니라 **단계**를 센다.

---

## 9. 인쇄 · PDF

- 배경은 흰색이다. 학생이 278쪽을 인쇄해도 잉크가 들지 않는다.
- PDF는 `npx decktape`가 헤드리스 Chrome으로 만든다. 브라우저 인쇄
  대화상자에 의존하지 않는다 — 환경마다 결과가 달라진다.
- `?export=1`이면 다운로드 버튼을 숨긴다.

---

## 10. 접근성

- 본문에 쓰는 색은 흰 배경 대비 **4.5:1 이상**이어야 한다.
  `--muted`(3.6:1)와 `--chart-orange`(3.7:1)는 큰 글자·도형 전용이다.
- 곡선을 색으로만 구분하지 않는다. **라벨을 함께 붙인다.**
- 모든 `svg.fig`에 `<title>`을, 모든 `<img>`에 뜻이 담긴 `alt`를 쓴다.
  "그래프"가 아니라 "총수요곡선이 오른쪽으로 이동해 물가가 오르는 그림"이라고 적는다.

---

## 11. 하지 않는 것

- 크림슨 푸터바, 전폭 구분선
- 파란 헤더 + 줄무늬 표 (오피스 기본값의 신호다)
- 그라데이션 배경, 그림자, 이모지 불릿
- 한 화면에 5색 이상
- 본문 세리프
- 슬라이드 HTML에 과목명·학기·날짜·쪽번호 하드코딩
- 좌표를 손으로 찍은 그래프
- 푸터와 겹치는 표·그림

---

## 부록. 지금의 구조

테마는 `tokens.css` → `cbnu.css` → `components.css` → `print.css` 네 파일이고,
읽는 순서가 곧 덮어쓰는 순서다. 변종이 없다 — 모든 강의가 같은 테마를 쓴다.

지면 개편은 2026-08 에 `jimyeon.css` 라는 별도 파일을 맨 뒤에 얹는 방식으로 시작해,
두 학기 18개 강의가 전부 옮겨진 뒤 이 네 파일로 흡수하고 지웠다.
그때 확인한 것을 남겨 둔다 — **CSS 가중치는 파일을 합칠 때 제일 먼저 깨지는 것**이다.

- **가중치를 올리면 개별 표가 못 이긴다.** 정렬과 세로 괘선은
  `:where(.reveal) table th`(0,1,1 미만)에 둬야 `.tbl-prod th`(0,1,1)가 오른쪽 정렬을,
  `.tbl-payoff td`가 흰 괘선을 되찾는다. 반대로 파란 헤더를 죽이는 배경·색은
  `.reveal table th`(0,1,2)여야 `.tbl-stagger th`(0,1,1)를 이긴다.
  **한 선택자에 몰아 적지 말고 두 층으로 나눠 적는다.**
- **`.curve`와 `.curve-i1`은 가중치가 같다(0,3,0).** `.curve`를 나중에 적으면
  계열 색까지 덮어버린다. `.curve`를 먼저 두고 계열마다 굵기까지 다시 적는다.
- **역할 클래스와 색 유틸리티를 겹쳐 붙이지 않는다.** `.emph.t-define` 처럼 둘을 함께
  붙인 자리가 셋 있었는데, `jimyeon.css`가 맨 뒤에 있던 탓에 역할 색이 이기고 있었다.
  즉 붙여 둔 `t-define`·`t-red`가 아무 일도 하지 않았다. 흡수하면서 그 죽은 클래스를
  떼어 화면은 그대로 두고 예외를 없앴다. **지금은 나중에 선언되는 색 유틸리티가 이긴다** —
  역할 색을 그대로 쓰려면 유틸리티를 붙이지 않으면 된다.

파일을 합치거나 규칙을 옮길 때는 렌더가 그대로인지 눈이 아니라 기계로 확인한다.
전 슬라이드의 계산스타일을 헤드리스로 떠서 전후를 대조하면 위의 회귀가 전부 잡힌다
(`check-figures.sh`는 좌표·넘침·곡선 색 충돌은 잡지만 색·활자 회귀는 잡지 못한다).

---

## 12. 발표 전용 (§1~§11에 더한다)

근거가 되는 CSS는 `_shared/theme/talk.css` 하나다. 앞의 넷(tokens · cbnu · components ·
print)은 강의노트 복사본이므로 **고치지 않는다.**

### 12.1 표지는 넉 줄이다

강의 표지는 [학기 라벨 → 제목 → 날짜] 셋이다. 발표 표지는 여기에 **누가 말하는가**가
더 붙어 넷이 된다. 넷 모두 `talk.json`이 채우며 슬라이드에 적지 않는다.

| 줄 | 클래스 | 데이터 속성 | 출처 | top |
|---|---|---|---|---|
| 행사명 | `.cover-event` | `data-talk-event` | `event` (없으면 `label`) | 196px |
| 제목 | `h1` | — | 슬라이드에 직접 쓴다 | 218px |
| 발표자·소속 | `.cover-byline` | `data-talk-byline` | `authors` · `affiliation` | 358px |
| 날짜·장소 | `.cover-date` | `data-talk-date` | `date` · `venue` | 414px |

- 제목 아래 짧은 크림슨 룰(`h1::after`)이 제목 두 줄 기준 341px에 온다. 발표자 줄은
  그 아래에 놓인다. **제목이 세 줄이면 룰이 발표자 줄을 밟는다** — 제목을 두 줄 안에 넣는다
  (§4.6과 같은 이유다).
- `authors`의 **첫 사람이 발표자**이며 표지에서 굵게 나온다(`.speaker`).
  공저 발표에서 누가 서 있는지가 표지의 정보다.
- 값이 비면 `talk-init.js`가 그 요소를 지운다. 빈 줄이 여백만 먹는 일이 없다.

### 12.2 로고는 줄로 놓는다

강의 표지는 학교 마크 하나를 오른쪽 위(828, 40)에 절대배치한다. 학회·자문 발표는
주최 기관 마크를 함께 요구할 때가 있으므로 `.logo-row`(오른쪽 정렬 flex)로 감싼다.

```html
<div class="logo-row">
  <img class="cover-logo" src="../../_shared/img/cbnu-logo.jpg" alt="충북대학교">
  <img src="../assets/ex-학회로고.png" alt="한국도시행정학회">
</div>
```

높이는 46px로 통일된다. 받은 로고는 그 발표의 `assets/`에 두고, 외부 유래이므로
`ex-` 접두사를 붙인다 (CLAUDE.md의 공개 판단 대상이다).

### 12.3 푸터는 "행사명 / 날짜 / 발표자"

강의는 "과목 / 학기 / 이름"이었다. 문구 자체는 `talk.json`의 `footer` 한 줄에서 오므로
CSS가 하는 일은 쪽번호 자리(46px)를 비우고 넘치면 말줄임으로 자르는 것뿐이다.
행사명이 길면 푸터에서 잘린다 — 자를 자리를 정하려면 `footer`를 직접 짧게 쓴다.

### 12.4 kicker는 기본으로 넣지 않는다

강의는 모든 본문 슬라이드에 "03. 집적의 경제"를 얹는다. 여러 강이 이어지므로
지금 몇 강인지가 정보다. 발표는 한 덱이 한 주제라 그 정보가 없다.
**`talk.json`에 `kicker`를 적었을 때만** 들어간다. 긴 발표에서 부(部)를 나눌 때만 쓴다.

### 12.5 시간 예산이 장수를 정한다

발표는 강의와 달리 시간이 잘려 있다. **분당 1~1.5장이 기준이다.**

| 발표 시간 | 장수 |
|---|---|
| 10분 | 8~15장 |
| 15분 | 12~22장 |
| 20분 | 15~30장 |
| 40분 | 30~60장 |

`talk.json`의 `minutes`를 먼저 적는다. 슬라이드를 열면 `talk-init.js`가
장당 몇 초인지를 콘솔에 찍는다. 20초 아래로 내려가면 장수를 줄인다.

누적 공개(`.fragment`)는 한 단계가 PDF 한 쪽이 되지만(§8) **발표 시간에서는
한 장으로 세지 않는다.** 단계마다 말이 붙기 때문이다. 예산을 짤 때는 단계 수로 센다.

### 12.6 마무리 슬라이드에는 연락처를 둔다

강의의 `.s-closing`(Q & A)을 그대로 쓰되 한 줄을 더 받는다.

```html
<section class="s-closing">
  <h2>감사합니다</h2>
  <p class="contact">권규상 · 충북대학교 도시공학과 · kks1104@chungbuk.ac.kr</p>
</section>
```

메일 주소를 넣을지는 청중에 따라 판단한다. 공개 배포하는 발표에는 넣지 않는 편이 낫다.
